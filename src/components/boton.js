import React from "react";
import "../styles/button.css";

export const BotonPurp = (props) => {
  return (
    <button type="button" className="p-2" id="bP">
      {props.title}
    </button>
  );
};
export const BotonW = (props) => {
  return (
    <button type="button" className="p-2" id="bW">
      {props.title}
    </button>
  );
};
export const BotonY = (props) => {
  return (
    <button type="button" className="p-2" id="bY">
      {props.title}
    </button>
  );
};
